# Babblu public source

This repository contains the Babblu interface. Never commit passwords, API keys, OAuth tokens, database credentials, private resumes, or real application records.

The current interface uses sample data and does not yet submit real job applications. Production integrations must use environment variables and secure server-side storage.
